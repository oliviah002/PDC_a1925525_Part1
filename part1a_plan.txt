INFORMATION ABOUT THE ASSIGNMENT:
•	Define the neighbouring ranks using next = (my_rank + 1) % comm_sz and previous = (my_rank - 1 + comm_sz) % comm_sz.
•	Begin each ring pass with the updated position block currently owned by the process.
•	Send the current block to the next process and receive a block from the previous process.
•	Place each received block into the correct global position-array location.
•	Continue until every process has received every required block.
•	Ensure the completed ring pass produces the same logical position array as the original MPI_Allgather call.
•	Remove the timestep use of MPI_Allgather.
•	Use point-to-point communication and avoid deadlock.
•	Track the original owner of each communicated block correctly.
•	Work for valid process counts, including the one-process case.
•	Validate the ring implementation against the supplied MPI solver using identical inputs.


Plan:
1. Remove the timestep use of MPI_Allgather.
2. Define the neighbouring ranks using next = (my_rank + 1) % comm_sz and previous = (my_rank - 1 + comm_sz) % comm_sz.
3. Use point-to-point communication and avoid deadlock (by using MPI_Sendrecv)
4. Create pointers to send_buffer and recieve_buffer, and calculate current offset
5. Check if comm_sz <=1 and if true, return, as noone to communcite with if only one processor lol
6. init. send buffer with updated coordinates of current process, allowing for buffer
7. for loop for communication "pass", from 1 -> (comm_sz - 1)
8. FREEEEEE pointer memory to not have memory leaks
9. TEST!!!!